const togglePopup = (isPopupVisible, setPopupVisible) => {
    setPopupVisible(!isPopupVisible);
    // Need to do something here with selectedPreference
    // setSelectedPreference(null);  // Reset to original state
  };